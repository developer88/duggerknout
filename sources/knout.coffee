class Knout

  # Constructor method
  constructor: (@options) ->
    #@check_options()

  create_route: ->

  prepare_markers: ->


  #check_options: ->
   # if !@options['api'] || !@options['dom']
   #   throw 'Cannot load Dugger because of wrong initial params'



# Let's roll!
root = exports ? this
root.Knout = Knout